{
  "config": {
    "step": {
      "user": {
        "title": "Chroma Presets",
        "description": "Static, color-theory-based lighting palettes - each bulb gets its own harmonized color."
      }
    },
    "abort": {
      "already_configured": "Chroma Presets is already configured."
    }
  }
}
